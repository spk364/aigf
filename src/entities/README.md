# entities

FSD layer for business entities (e.g. User, Companion, Message).
Each entity encapsulates its model type, API slice, and UI card.
Do not import from `features`, `widgets`, or `pages` here.
